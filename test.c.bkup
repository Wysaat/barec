typedef int t;

typedef struct list list;

struct list {
    int type;
    void *content;
    list *prev;
    list *next;
};

struct nested {
    struct list a;
    int b;
};

int is_int(char *token) {
    while (*token) {
        if (!(*token >= '0' && *token <= '9'))
            return 0;
        token++;
    }
    return 1;
}

int tom;

int sum_int(char *token) {
    int retval = 0;
    while (*token)
        retval += *token - '0';
    return retval;
}

int main()
{
    char *a = "104342";
    int b = 0;
    // static struct nested c;
    // c.a.type = 4;
    if (is_int(a))
        b = sum_int(a);
    b;
    return a;
}